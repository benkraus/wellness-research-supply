# Local development
# Backend URL (Medusa). Used by server utilities and SDK.
PUBLIC_MEDUSA_API_URL='http://localhost:9000'

# Public URL of this storefront (used in metadata and redirects)
STOREFRONT_URL='http://localhost:7890'

# Publishable API key (Medusa). Seed creates a "Webshop" key.
# You can fetch it from the backend at: GET http://localhost:9000/key-exchange
MEDUSA_PUBLISHABLE_KEY='pk_'

# Search (Meilisearch)
PUBLIC_SEARCH_ENDPOINT='http://localhost:7700'
PUBLIC_SEARCH_API_KEY='your_search_key_here'
SEARCH_INDEX_NAME='products'

# PostHog analytics
PUBLIC_POSTHOG_KEY='phc_FejEXnMciKRgMCT4jgrr81DFTEEwkXTFKZWQduNMwJp'
# Optional: set to your region host (e.g. https://us.i.posthog.com or https://eu.i.posthog.com)
# This repo uses a reverse proxy by default.
PUBLIC_POSTHOG_HOST='https://posthog-reverse-proxy-production-dc08.up.railway.app'

# Klaviyo onsite tracking
KLAVIYO_SITE_ID=''

# Klaviyo private API key (for server-side newsletter subscriptions)
KLAVIYO_PRIVATE_API_KEY=''
